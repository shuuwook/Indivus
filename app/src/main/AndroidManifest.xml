<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="indivus.cosmos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".application.Indivus"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- activity_splash -->
        <activity android:name=".SplashActivity" >
        </activity>
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignUpSelectCategoryActivity" >
        </activity>
        <activity android:name=".PostActivity"></activity>
        <activity android:name=".SeriesActivity" />
        <activity android:name=".ReplyActivity" />
        <activity android:name=".ReplyDetailActivity" />
        <activity android:name=".CreateActivity"></activity>
        <activity android:name=".CreateContentActivity" />
        <activity android:name=".CollectionActivity"></activity>
        <activity android:name=".ClosetActivity"></activity>
    </application>

</manifest>